[package]
name = "sketch-spgemm"
version = "0.7.0"
edition = "2021"
description = "Adaptive SketchSpGEMM with moment recovery, masking, fingerprints, and automatic exact/sketch selection"
license = "MIT OR Apache-2.0"

[profile.release]
lto = "thin"
codegen-units = 1

[dependencies]

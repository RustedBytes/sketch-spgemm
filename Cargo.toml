[package]
name = "sketch-spgemm"
version = "0.5.0"
edition = "2021"
description = "Cached deterministic nested sparse-recovery SpGEMM with adaptive rectangular kernels"
license = "MIT OR Apache-2.0"

[profile.release]
lto = "thin"
codegen-units = 1

[dependencies]

[package]
name = "sketch-spgemm"
version = "0.6.1"
edition = "2021"
description = "Practical nested sparse-recovery SpGEMM with moment peeling, support masking, and adaptive scheduling"
license = "MIT OR Apache-2.0"

[profile.release]
lto = "thin"
codegen-units = 1

[dependencies]
